<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash/window theme before Compose takes over. Dark only. -->
    <style name="Theme.ThorRomButler" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/thor_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
